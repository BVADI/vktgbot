# Copy to file docker-compose.yml
version: '3'

services:
  trialsportru: # name docker
    build: ../
    image: trialsportrubot  # name docker image
    volumes:
      - ../logs:/code/logs
      - ../last_id.txt:/code/last_id.txt
